version: '3.8'

services:

  lavalink:
    container_name: lavalink
    build:
      dockerfile: lavalink/Dockerfile
    volumes:
      - ./lavalink:/usr/src/lavalink

  bot:
    container_name: bot
    build:
      dockerfile: bot/Dockerfile
    environment:
      ENV_PATH: ./env/test.env
      MUSIC_ONLY: false
    volumes:
      - ./bot:/usr/src/bot